with "../../sdl3ada.gpr";

project Video_Foundation_Smoke is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "../../obj/debug/video_foundation_smoke";
   for Exec_Dir use "../../bin";
   for Main use ("video_foundation_smoke.adb");

   package Builder is
      for Executable ("video_foundation_smoke.adb") use "video_foundation_smoke";
   end Builder;

   package Linker is
      case SDL3Ada.Platform is
         when "macosx" =>
            for Default_Switches ("Ada") use ("-F" & SDL3Ada.SDL3_Framework_Dir, "-Wl,-rpath," & SDL3Ada.SDL3_Framework_Dir);

         when others =>
            null;
      end case;
   end Linker;
end Video_Foundation_Smoke;
