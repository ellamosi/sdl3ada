with "../../sdl3ada.gpr";

project Device_Smoke is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "../../obj/debug/device_smoke";
   for Exec_Dir use "../../bin";
   for Main use ("device_smoke.adb");

   package Builder is
      for Executable ("device_smoke.adb") use "device_smoke";
   end Builder;

   package Linker is
      case SDL3Ada.Platform is
         when "macosx" =>
            for Default_Switches ("Ada") use ("-F" & SDL3Ada.SDL3_Framework_Dir, "-Wl,-rpath," & SDL3Ada.SDL3_Framework_Dir);

         when others =>
            null;
      end case;
   end Linker;
end Device_Smoke;
