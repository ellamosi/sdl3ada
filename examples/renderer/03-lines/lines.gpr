with "../../../sdl3ada.gpr";

project Lines is

   for Languages use ("Ada");
   for Source_Dirs use (".");
   for Object_Dir use "../../../obj/debug/lines";
   for Exec_Dir use "../../../bin";
   for Main use ("lines.adb");

   package Builder is
      for Executable ("lines.adb") use "lines";
   end Builder;

   package Linker is
      case SDL3Ada.Platform is
         when "macosx" =>
            for Default_Switches ("Ada") use ("-F" & SDL3Ada.SDL3_Framework_Dir, "-Wl,-rpath," & SDL3Ada.SDL3_Framework_Dir);

         when others =>
            null;
      end case;
   end Linker;
end Lines;
